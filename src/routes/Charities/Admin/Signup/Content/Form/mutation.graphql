mutation(
  $ein: String!,
  $email: String!,
  $name: String!,
  $representative: String!,
  $url: String!
) {
  charityCreate(
    ein: $ein,
    email: $email,
    name: $name,
    representative: $representative,
    url: $url
  )
}
