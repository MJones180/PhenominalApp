mutation(
  $amount: Int!
  $events: [ID!]
) {
  donation(
    amount: $amount
    events: $events
  ) {
    amount
    dotTotal
    dotsGained
    loopCount
    loopsGained
    transactions {
      balance
      event
      id
    }
  }
}
