mutation(
  $amount: Int!
  $eventID: ID!
) {
  donation(
    amount: $amount
    eventID: $eventID
  ) {
    amount
    balance
    charityName
    dotTotal
    dotsGained
    loopCount
    loopGained
    transactionID
  }
}
